{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "http://example.com/set_machine_pose.json",
    "type": "object",
    "title": "Machine Pose Update Schema",
    "description": "Schema for placing a machine to a different pose..",
    "required": ["name", "rotation", "zone"],
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the machine."
        },
        "rotation": {
            "type": "number",
            "description": "The 45 degree discretized orientation of the machine."
        },
        "zone": {
            "type": "string",
            "description": "The zone name where the machine should be placed."
        }
    }
}
